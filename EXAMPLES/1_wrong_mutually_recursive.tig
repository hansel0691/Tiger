/*Error: Tipos mutuamente recursivos que no están en un mismo bloque, 
		por lo que el record asume que no existe el tipo del elemento*/
let
/* define a tree */
type tree ={key: int, children: treelist}
var d:int :=0
type treelist = {hd: tree, tl: treelist}

in
	d
end
